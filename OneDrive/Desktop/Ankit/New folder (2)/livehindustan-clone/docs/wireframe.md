# Wireframe / Sketch

This wireframe represents the simplified front-page layout.

You can add a hand-drawn screenshot file to `public/` (for example, `public/wireframe.png`) and reference it here.

## ASCII Wireframe
```
----------------------------------------------------
| HEADER (branding / nav)                           |
----------------------------------------------------
| TOP STORIES (Main hero) | LATEST (side column)   |
| (large image + title)    | - Latest article 1     |
|--------------------------| - Latest article 2     |
| Grid of cards (2-column) | - Latest article 3     |
| (small image + title)    | - ...                  |
----------------------------------------------------
| Footer                                            |
----------------------------------------------------
```

## Suggestions for a simple hand-drawn wireframe
- Draw a wide header block at top with the site name and nav items.
- On the left, draw one large block representing the hero/top story; add 2–4 smaller blocks below for additional top stories.
- On the right, draw column with smaller blocks representing 'Latest' sections (quick article links).
- Add a footer bar with copyright information at the bottom.

## Notes
- This skeleton maps directly to the pages and components implemented in the codebase (`Header`, `ArticleCard`, `index.js`, `articles/[id].js`).
